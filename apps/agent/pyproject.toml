[project]
name = "devrimo-agent"
version = "0.1.0"
description = "Devrimo agent core — FastAPI + hand-written tool-calling loop"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]
